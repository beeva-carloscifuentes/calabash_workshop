# Create Calabash android & iOS with Page Object Pattern

The script generate a functional structure with demo files that you can run.

Run the next command and enjoy it:

```bash
$ sh create_calabash_pop_project.sh
```
